version: 2
jobs:
  build-and-test:
    macOS:
      xcode: "11.3.4"
    steps:
      - checkout
      - run: uname -a
workflows:
  version: 2

  feature_branch_checks:
    jobs:
      - build-and-test
