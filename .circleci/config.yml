version: 2
jobs:
  build-and-test:
    macOS:
      xcode: "12.2.0"
    steps:
      - checkout
      - run: uname -a
workflows:
  version: 2

  feature_branch_checks:
    jobs:
      - build-and-test
